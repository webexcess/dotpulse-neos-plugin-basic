prototype(Page).htmlTag.attributes {
	data-version = ${baseConfiguration.VersionOfAssets}
	class = Fusion:RawArray {
		live = ${node.context.inBackend ? 'in-backend' : 'live'}
		javascript = 'no-js'
	}
}
